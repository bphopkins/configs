-- Generated by snipgen 1 from polyhedra.cwl; do not edit, regenerate.
-- source: TeXstudio completion/polyhedra.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: f4a11125042611b9f878be388b37eb46cd966c3910a6c5d565b9cffd67251b6b
-- cwl-header:
--   # polyhedra package
--   # Matthew Bertucci 2023/11/08 for v0.3
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "polyhedra",
  includes = { "xcolor", "xcolor-solarized", "tikz", "tikz-3dplot", "xstring", "tikzlibraryshapes", "tikzlibrarypatterns" },
  lists = {},
  snippets = {
    S({ trig = "\\polyhedron", dscr = "\\begin{polyhedron}{keyvals}" }, { t("\\begin{polyhedron}{"), i(1, "keyvals"), t({ "}", "\t" }), i(2), t({ "", "\\end{polyhedron}" }) }, "\\pictureHighlight"),
    S({ trig = "\\point", dscr = "{(x,y,z)}{name}" }, { t("\\point{("), i(1, "x,y,z"), t(")}{"), i(2, "name"), t("}") }),
    S({ trig = "\\axes", dscr = "{keyvals}" }, { t("\\axes{"), i(1, "keyvals"), t("}") }),
    S({ trig = "\\vertex", dscr = "{keyvals}" }, { t("\\vertex{"), i(1, "keyvals"), t("}") }),
    S({ trig = "\\edge", dscr = "{keyvals}" }, { t("\\edge{"), i(1, "keyvals"), t("}") }),
    S({ trig = "\\ray", dscr = "{keyvals}" }, { t("\\ray{"), i(1, "keyvals"), t("}") }),
    S({ trig = "\\cone", dscr = "{keyvals}" }, { t("\\cone{"), i(1, "keyvals"), t("}") }),
    S({ trig = "\\polygon", dscr = "{keyvals}" }, { t("\\polygon{"), i(1, "keyvals"), t("}") }),
    S({ trig = "\\halfspace", dscr = "{keyvals}" }, { t("\\halfspace{"), i(1, "keyvals"), t("}") }),
    S({ trig = "\\hyperplane", dscr = "{keyvals}" }, { t("\\hyperplane{"), i(1, "keyvals"), t("}") }),
  },
  keyvals = {
    {
      targets = "\\begin{polyhedron}",
      lines = {
        "dim=%<integer%>",
        "phi=%<degrees%>",
        "theta=%<degrees%>",
      },
    },
    {
      targets = "\\axes",
      lines = {
        "x={%<num1,num2%>}",
        "y={%<num1,num2%>}",
        "z={%<num1,num2%>}",
        "xlabel=%<text%>",
        "ylabel=%<text%>",
        "zlabel=%<text%>",
        "labels=#true,false",
      },
    },
    {
      targets = "\\vertex,\\edge,\\ray,\\cone,\\polygon,\\halfspace,\\hyperplane",
      lines = {
        "color=#%color",
        "status=#normal,focus,alert,open",
        "opacity=%<factor%>",
        "thickness=%<number%>",
        "pattern={%<pattern%>}",
      },
    },
    {
      targets = "\\vertex",
      lines = {
        "point={%<point%>}",
        "text=%<text%>",
        "textcolor=#%color",
        "anchora=#north,south,east,west",
        "anchorb=#above,below,left,right",
      },
    },
    {
      targets = "\\edge,\\ray",
      lines = {
        "points={%<point1,point2%>}",
      },
    },
    {
      targets = "\\cone",
      lines = {
        "vertex={%<point%>}",
        "generators={%<list of point%>}",
      },
    },
    {
      targets = "\\polygon",
      lines = {
        "points={%<list of point%>}",
        "boundary=#true,false",
      },
    },
    {
      targets = "\\halfspace,\\hyperplane",
      lines = {
        "points={%<list of point%>}",
        "patterncolor=#%color",
      },
    },
  },
}
