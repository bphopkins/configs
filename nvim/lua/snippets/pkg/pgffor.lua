-- Generated by snipgen 1 from pgffor.cwl; do not edit, regenerate.
-- source: TeXstudio completion/pgffor.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: d5d14f2058e53be8622f2be7a9886166ecc44340c9335ad7f051366bf7a95b56
-- cwl-header:
--   # pgffor package
--   # muzimuzhi, 8 Aug 2020, pgffor in tikz v3.1.5b
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "pgffor",
  includes = { "pgfrcs", "pgfkeys", "pgfmath" },
  lists = {},
  snippets = {
    S({ trig = "\\foreach", dscr = "\\macro in {list}{code}" }, { t("\\foreach "), i(1, "\\macro"), t(" in {"), i(2, "list"), t("}{"), i(3, "code"), t("}") }),
    S({ trig = "\\foreach", dscr = "[options] \\macro in {list}{code}" }, { t("\\foreach ["), i(1, "options"), t("] "), i(2, "\\macro"), t(" in {"), i(3, "list"), t("}{"), i(4, "code"), t("}") }),
    S({ trig = "\\breakforeach", dscr = "∗", priority = 900 }, { t("\\breakforeach") }, "*"),
  },
  keyvals = {
    {
      targets = "\\foreach#c",
      lines = {
        "var=%<variable%>",
        "evaluate=%<variable%>",
        "evaluate=%<variable%> as %<macro%>",
        "evaluate=%<variable%> as %<macro%> using %<formula%>",
        "remember=%<variable%> as %<macro%>",
        "remember=%<variable%> as %<macro%> (initially %<value%>)",
        "count=%<macro%>",
        "count=%<macro%> from %<value%>",
        "parse#true,false",
        "expand list#true,false",
      },
    },
  },
}
