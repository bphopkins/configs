-- Generated by snipgen 1 from tikzlibraryspy.cwl; do not edit, regenerate.
-- source: TeXstudio completion/tikzlibraryspy.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: 328470bf0d07451def957d596aa65557e289bf628d68850d029ea4b6d58fd402
-- cwl-header:
--   # spy tikzlibrary
--   # 2022/11/21 for v3.1.9a
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "tikzlibraryspy",
  includes = {},
  lists = {},
  snippets = {
    S({ trig = "\\spy", dscr = "on 〈coordinate〉 in node 〈node spec〉;" }, { t("\\spy on "), i(1, "〈coordinate〉"), t(" in node "), i(2, "〈node spec〉"), t(";") }),
    S({ trig = "\\spy", dscr = "[options] on 〈coordinate〉 in node 〈node spec〉;" }, { t("\\spy["), i(1, "options"), t("] on "), i(2, "〈coordinate〉"), t(" in node "), i(3, "〈node spec〉"), t(";") }),
  },
  keyvals = {
    {
      targets = "\\begin{tikzpicture}#c,\\tikz#c,\\begin{scope}#c,\\tikzset#c,\\scoped#c",
      lines = {
        "spy scope={%<options%>}",
        "every spy in node/.style={%<options%>}",
        "spy connection path=%<code%>",
        "every spy on node/.style={%<options%>}",
        "spy using outlines={%<options%>}",
        "spy using overlays={%<options%>}",
      },
    },
    {
      targets = "\\spy#c",
      lines = {
        "size=##L",
        "height=##L",
        "width=##L",
        "connect spies",
        "%color",
      },
    },
  },
}
