-- Generated by snipgen 1 from tikz-qtree.cwl; do not edit, regenerate.
-- source: TeXstudio completion/tikz-qtree.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: 7d1190e55ec7f410c16c691eb3528eb91d1c317f2ca352ecd088966680fce1e5
-- cwl-header:
--   # tikz-qtree package
--   # Matthew Bertucci 4/13/2022 for v1.2
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "tikz-qtree",
  includes = { "tikz", "pgftree" },
  lists = {},
  snippets = {
    S({ trig = "\\Tree", dscr = "[qtree code]" }, { t("\\Tree["), i(1, "qtree code"), t("]") }),
    S({ trig = "\\edge", dscr = ";" }, { t("\\edge;") }),
    S({ trig = "\\edge", dscr = "[options];" }, { t("\\edge["), i(1, "options"), t("];") }),
    S({ trig = "\\edge", dscr = "node [options] {label};" }, { t("\\edge node ["), i(1, "options"), t("] {"), i(2, "label"), t("};") }),
    S({ trig = "\\edge", dscr = "[options] node {label};" }, { t("\\edge["), i(1, "options"), t("] node {"), i(2, "label"), t("};") }),
    S({ trig = "\\edge", dscr = "[options] node[options] {label};" }, { t("\\edge["), i(1, "options"), t("] node["), i(2, "options"), t("] {"), i(3, "label"), t("};") }),
  },
  keyvals = {
    {
      targets = "\\tikzset#c,\\begin{tikzpicture}#c,\\begin{scope}#c",
      lines = {
        "level distance=##L",
        "sibling distance=##L",
        "grow=#up,down,left,right",
        "grow'=#up,down,left,right",
        "level %<n%>+/.style={%<options%>}",
        "interior/.style={%<options%>}",
        "frontier/.style={%<options%>}",
      },
    },
    {
      targets = "\\tikzset#c",
      lines = {
        "every tree node/.style={%<options%>}",
        "every internal node/.style={%<options%>}",
        "every leaf node/.style={%<options%>}",
        "every level %<n%> node/.style={%<options%>}",
        "edge from parent/.append style={%<options%>}",
      },
    },
  },
}
