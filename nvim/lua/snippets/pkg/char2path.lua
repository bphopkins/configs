-- Generated by snipgen 1 from char2path.cwl; do not edit, regenerate.
-- source: TeXstudio completion/char2path.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: 049b54bbaf4af7089c0bf83a8b65818eee16a3bd36c7da045cd29c9611499208
-- cwl-header:
--   # char2path package
--   # Matthew Bertucci 2025/08/04 for v1.0.0
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "char2path",
  includes = { "tikz", "etoolbox" },
  lists = {},
  snippets = {
    S({ trig = "\\chartopath", dscr = "{string}" }, { t("\\chartopath{"), i(1, "string"), t("}") }),
    S({ trig = "\\chartopath", dscr = "[keyvals]{string}" }, { t("\\chartopath["), i(1, "keyvals"), t("]{"), i(2, "string"), t("}") }),
    S({ trig = "\\chartoclip", dscr = "{char}" }, { t("\\chartoclip{"), i(1, "char"), t("}") }),
    S({ trig = "\\chartoclip", dscr = "[keyvals]{char}" }, { t("\\chartoclip["), i(1, "keyvals"), t("]{"), i(2, "char"), t("}") }),
    S({ trig = "\\ctpdata", dscr = "{font name}{char} ∗", priority = 900 }, { t("\\ctpdata{"), i(1, "font name"), t("}{"), i(2, "char"), t("}") }, "*"),
  },
  keyvals = {
    {
      targets = "\\usepackage/char2path#c",
      lines = {
        "scale=%<size%>",
      },
    },
    {
      targets = "\\chartopath",
      lines = {
        "font=#lmr,lms,lmm",
        "draw=#%color",
        "fill=#%color",
        "outline=##L",
        "scale=%<factor%>",
        "hfading={%<color list%>}",
        "vfading={%<color list%>}",
        "rescan#true,false",
      },
    },
    {
      targets = "\\chartoclip",
      lines = {
        "font=#lmr,lms,lmm",
        "image=%<imagefile%>",
        "scale=%<factor%>",
        "anchor=%<TikZ anchor%>",
        "offset={%<(x,y)%>}",
        "height=##L",
      },
    },
  },
}
