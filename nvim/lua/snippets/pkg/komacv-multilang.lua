-- Generated by snipgen 1 from komacv-multilang.cwl; do not edit, regenerate.
-- source: TeXstudio completion/komacv-multilang.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: 88dd8a6990c561cfe1421decf7422b8920411e443b53d037d6a96b1a29b33da2
-- cwl-header:
--   # komacv-multilang package
--   # Matthew Bertucci 1/20/2022 for v0.9.2
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "komacv-multilang",
  includes = { "multilang", "multilang-tags", "multilang-sect", "datetime2", "datetime2-calc", "translations" },
  lists = {},
  snippets = {
    S({ trig = "\\BasicEntry", dscr = "{data}" }, { t("\\BasicEntry{"), i(1, "data"), t("}") }),
    S({ trig = "\\CommentedEntry", dscr = "{data}" }, { t("\\CommentedEntry{"), i(1, "data"), t("}") }),
    S({ trig = "\\DoubleEntry", dscr = "{data}" }, { t("\\DoubleEntry{"), i(1, "data"), t("}") }),
    S({ trig = "\\EducationEntry", dscr = "{data}" }, { t("\\EducationEntry{"), i(1, "data"), t("}") }),
    S({ trig = "\\EmploymentEntry", dscr = "{data}" }, { t("\\EmploymentEntry{"), i(1, "data"), t("}") }),
    S({ trig = "\\AchievementEntry", dscr = "{data}" }, { t("\\AchievementEntry{"), i(1, "data"), t("}") }),
    S({ trig = "\\EntryListItem", dscr = "{data}" }, { t("\\EntryListItem{"), i(1, "data"), t("}") }),
    S({ trig = "\\EntryListDblItem", dscr = "{data}" }, { t("\\EntryListDblItem{"), i(1, "data"), t("}") }),
    S({ trig = "\\Item", dscr = "{data}" }, { t("\\Item{"), i(1, "data"), t("}") }),
  },
  keyvals = {
    {
      targets = "\\usepackage/komacv-multilang#c",
      lines = {
        "languages={%<lang1,lang2,...%>}",
      },
    },
  },
}
