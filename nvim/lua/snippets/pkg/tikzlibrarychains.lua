-- Generated by snipgen 1 from tikzlibrarychains.cwl; do not edit, regenerate.
-- source: TeXstudio completion/tikzlibrarychains.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: 66403ea1b4108f574dc9cf7f7c0845ff7a41a5e65eb792ef39c1a54efae1317e
-- cwl-header:
--   # chains tikzlibrary
--   # 2022/11/20 for v3.1.9a
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "tikzlibrarychains",
  includes = { "tikzlibrarypositioning" },
  lists = {},
  snippets = {
    S({ trig = "\\chainin", dscr = "(node name);" }, { t("\\chainin("), i(1, "node name"), t(");") }),
    S({ trig = "\\chainin", dscr = "(node name)[options];" }, { t("\\chainin("), i(1, "node name"), t(")["), i(2, "options"), t("];") }),
    S({ trig = "\\tikzchaincount", dscr = "∗", priority = 900 }, { t("\\tikzchaincount") }, "*"),
    S({ trig = "\\tikzchaincurrent", dscr = "∗", priority = 900 }, { t("\\tikzchaincurrent") }, "*"),
    S({ trig = "\\tikzchainprevious", dscr = "∗", priority = 900 }, { t("\\tikzchainprevious") }, "*"),
  },
  keyvals = {
    {
      targets = "\\begin{tikzpicture}#c,\\tikz#c,\\begin{scope}#c,\\scoped#c",
      lines = {
        "start chain",
        "chain default direction=%<direction%>",
      },
    },
    {
      targets = "\\node#c,\\coordinate#c,\\nodepart#c,\\pic#c,\\chainin#c",
      lines = {
        "continue chain",
        "continue chain=%<direction%>",
        "continue chain=%<〈chain name〉〈direction〉%>",
        "on chain",
        "on chain=%<direction%>",
        "on chain=%<〈chain name〉〈direction〉%>",
        "join",
        "join=with %<with%> by {%<options%>}",
      },
    },
    {
      targets = "\\begin{tikzpicture}#c,\\tikz#c,\\begin{scope}#c,\\tikzset#c,\\scoped#c",
      lines = {
        "every on chain/.style={%<options%>}",
        "every join/.style={%<options%>}",
      },
    },
  },
}
