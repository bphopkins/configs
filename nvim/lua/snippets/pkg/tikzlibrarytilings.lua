-- Generated by snipgen 1 from tikzlibrarytilings.cwl; do not edit, regenerate.
-- source: TeXstudio completion/tikzlibrarytilings.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: 6395ba76ac8bd606290168ace572de9c8c6f0cb1613e6330349c678705ff5965
-- cwl-header:
--   # tilings tikzlibrary
--   # 2023/06/06 for v2.0
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "tikzlibrarytilings",
  includes = { "tikzlibraryspath3" },
  lists = {},
  snippets = {
    S({ trig = "\\DefineTile", dscr = "{name}{sides}{vertices}" }, { t("\\DefineTile{"), i(1, "name"), t("}{"), i(2, "sides"), t("}{"), i(3, "vertices"), t("}") }, "s#%penrosetilename"),
    S({ trig = "\\BakeTile", dscr = "{name}" }, { t("\\BakeTile{"), i(1, "name"), t("}") }),
    S({ trig = "\\TilingDecomposition", dscr = "{type}{level}{seed}" }, { t("\\TilingDecomposition{"), i(1, "type"), t("}{"), i(2, "level"), t("}{"), i(3, "seed"), t("}") }),
    S({ trig = "\\TilingDecomposition", dscr = "[TikZ keys]{type}{level}{seed}" }, { t("\\TilingDecomposition["), i(1, "TikZ keys"), t("]{"), i(2, "type"), t("}{"), i(3, "level"), t("}{"), i(4, "seed"), t("}") }),
  },
  keyvals = {
    {
      targets = "\\BakeTile#c,\\pic#c",
      lines = {
        "%penrosetilename",
      },
    },
    {
      targets = "\\TilingDecomposition#c",
      lines = {
        "kite",
        "rhombus",
        "pentagon",
        "ktriangle",
        "rtriangle",
      },
    },
    {
      targets = "\\begin{tikzpicture}#c,\\tikz#c,\\begin{scope}#c,\\tikzset#c,\\scoped#c",
      lines = {
        "every tile/.style={%<TikZ keys%>}",
        "tile %<<n>%>/.style={%<TikZ keys%>}",
        "every circle arc/.style={%<TikZ keys%>}",
        "every long arc/.style={%<TikZ keys%>}",
        "tiling step=##L",
        "first tile/.style={%<TikZ keys%>}",
      },
    },
    {
      targets = "\\pic#c",
      lines = {
        "align with=%<tile%> along %<edge%>",
        "align with=%<tile%> along %<edge%> using %<number%>",
      },
    },
    {
      targets = "\\path#c",
      lines = {
        "save tiling path=%<edge%>",
      },
    },
    {
      targets = "\\tikzset#c",
      lines = {
        "clone tiling side path={%<target%>}{%<source%>}",
        "clone tile path={%<target%>}{%<source%>}",
      },
    },
  },
}
