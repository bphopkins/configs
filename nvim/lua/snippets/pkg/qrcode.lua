-- Generated by snipgen 1 from qrcode.cwl; do not edit, regenerate.
-- source: TeXstudio completion/qrcode.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: 45c99a8d8032bc78fc57349797fac3cbc685e3dfe701afc5e28406db9dde6c60
-- cwl-header:
--   # qrcode package
--   # Matthew Bertucci 2024/05/26 for v1.51
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "qrcode",
  includes = { "xcolor", "xkeyval" },
  lists = {},
  snippets = {
    S({ trig = "\\qrcode", dscr = "{URL}" }, { t("\\qrcode{"), i(1, "URL"), t("}") }, "U"),
    S({ trig = "\\qrcode", dscr = "[options]{URL}" }, { t("\\qrcode["), i(1, "options"), t("]{"), i(2, "URL"), t("}") }, "U"),
    S({ trig = "\\qrcode*", dscr = "{URL}" }, { t("\\qrcode*{"), i(1, "URL"), t("}") }, "U"),
    S({ trig = "\\qrcode*", dscr = "[options]{URL}" }, { t("\\qrcode*["), i(1, "options"), t("]{"), i(2, "URL"), t("}") }, "U"),
    S({ trig = "\\qrset", dscr = "{options}" }, { t("\\qrset{"), i(1, "options"), t("}") }),
  },
  keyvals = {
    {
      targets = "\\usepackage/qrcode#c",
      lines = {
        "nolinks",
        "draft",
        "final",
        "forget",
      },
    },
    {
      targets = "\\qrcode,\\qrset,\\qrcode*",
      lines = {
        "height=##L",
        "level=#L,M,Q,H",
        "version=",
        "tight",
        "padding",
        "draft",
      },
    },
    {
      targets = "\\qrcode,\\qrset",
      lines = {
        "link",
        "nolink",
        "hyperlink",
      },
    },
  },
}
