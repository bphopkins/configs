-- Generated by snipgen 1 from envelope-letter.cwl; do not edit, regenerate.
-- source: TeXstudio completion/envelope-letter.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: 8712afe1f585f94e3d419125af7426a6fd71a95235e86e3e54cd7a8b48fc05ae
-- cwl-header:
--   # envelope-letter package
--   # Matthew Bertucci 2025/08/29 for v1.1
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "envelope-letter",
  includes = { "tikz", "pgfopts", "geometry", "graphics", "pifont", "fp" },
  lists = {},
  snippets = {
    S({ trig = "\\envelopesetup", dscr = "{options}" }, { t("\\envelopesetup{"), i(1, "options"), t("}") }),
    S({ trig = "\\makeenvelope", dscr = "" }, { t("\\makeenvelope") }),
    S({ trig = "\\makeenvelope", dscr = "[options]" }, { t("\\makeenvelope["), i(1, "options"), t("]") }),
    S({ trig = "\\setstamp", dscr = "{code}" }, { t("\\setstamp{"), i(1, "code"), t("}") }),
  },
  keyvals = {
    {
      targets = "\\usepackage/envelope-letter#c,\\envelopesetup,\\makeenvelope",
      lines = {
        "grammage=%<number%>",
        "fold lines mode=#0,1,2",
        "no lines",
        "ticks",
        "lines",
        "steps",
        "no steps",
        "instructions",
        "no instructions",
        "full",
        "eco",
        "empty",
        "auto",
        "manual",
        "to=%<text%>",
        "from=%<text%>",
      },
    },
  },
}
