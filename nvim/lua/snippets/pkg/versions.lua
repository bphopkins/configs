-- Generated by snipgen 1 from versions.cwl; do not edit, regenerate.
-- source: TeXstudio completion/versions.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: 9da10a2dcc6f6a594237f741dcc92fd365a728f5f64bdaca1cbb8ba79b0c094d
-- cwl-header:
--   # versions package
--   # Matthew Bertucci 1/12/2021 for v0.55
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "versions",
  includes = {},
  lists = {},
  snippets = {
    S({ trig = "\\markversion", dscr = "{version}" }, { t("\\markversion{"), i(1, "version"), t("}") }, "N"),
    S({ trig = "\\includeversion", dscr = "{version}" }, { t("\\includeversion{"), i(1, "version"), t("}") }, "N"),
    S({ trig = "\\excludeversion", dscr = "{version}" }, { t("\\excludeversion{"), i(1, "version"), t("}") }, "N"),
    S({ trig = "\\comment", dscr = "\\begin{comment}" }, { t({ "\\begin{comment}", "\t" }), i(1), t({ "", "\\end{comment}" }) }),
    S({ trig = "\\processifversion", dscr = "{version}{code}" }, { t("\\processifversion{"), i(1, "version"), t("}{"), i(2, "code"), t("}") }),
    S({ trig = "\\versionmessage", dscr = "{version}{type}" }, { t("\\versionmessage{"), i(1, "version"), t("}{"), i(2, "type"), t("}") }),
  },
  keyvals = {
    {
      targets = "\\usepackage/versions#c",
      lines = {
        "nogroup",
        "tracing",
      },
    },
    {
      targets = "\\versionmessage#c",
      lines = {
        "excluded",
        "included",
        "included with marks",
      },
    },
  },
}
