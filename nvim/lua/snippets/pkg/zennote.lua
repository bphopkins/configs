-- Generated by snipgen 1 from zennote.cwl; do not edit, regenerate.
-- source: TeXstudio completion/zennote.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: abf5e49a7566b7f414be41984517297274e9eff112f0ef0da0be352246df5fca
-- cwl-header:
--   # zennote package
--   # Matthew Bertucci 2023/01/16 for v1.0.0
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "zennote",
  includes = { "tcolorbox", "tcolorboxlibrarymost" },
  lists = {},
  snippets = {
    S({ trig = "\\titlebox", dscr = "{settings}" }, { t("\\titlebox{"), i(1, "settings"), t("}") }),
    S({ trig = "\\noteframe", dscr = "\\begin{noteframe}" }, { t({ "\\begin{noteframe}", "\t" }), i(1), t({ "", "\\end{noteframe}" }) }),
    S({ trig = "\\noteframe", dscr = "\\begin{noteframe}[options]" }, { t("\\begin{noteframe}["), i(1, "options"), t({ "]", "\t" }), i(2), t({ "", "\\end{noteframe}" }) }),
    S({ trig = "\\thenotenumber", dscr = "∗", priority = 900 }, { t("\\thenotenumber") }, "*"),
  },
  keyvals = {
    {
      targets = "\\titlebox",
      lines = {
        "top-left={%<text%>}",
        "top-right={%<text%>}",
        "bottom-left={%<text%>}",
        "bottom-right={%<text%>}",
        "notenumber=%<integer%>",
        "topic={%<text%>}",
        "type={%<text%>}",
      },
    },
    {
      targets = "\\begin{noteframe}",
      lines = {
        "color=#%color",
        "style=#zero,one,two",
        "title={%<text%>}",
      },
    },
  },
}
