-- Generated by snipgen 1 from unicodefonttable.cwl; do not edit, regenerate.
-- source: TeXstudio completion/unicodefonttable.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: 7dbb2a54020364f2d119578a275ab0b286847651ab211f1f00931d78b260d59c
-- cwl-header:
--   # unicodefonttable package
--   # Matthew Bertucci 2022/11/13 for v1.0g
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "unicodefonttable",
  includes = { "xcolor", "l3keys2e", "longtable", "booktabs", "caption", "fontspec" },
  lists = {},
  snippets = {
    S({ trig = "\\displayfonttable", dscr = "{font name}{font features}" }, { t("\\displayfonttable{"), i(1, "font name"), t("}{"), i(2, "font features"), t("}") }),
    S({ trig = "\\displayfonttable", dscr = "[options]{font name}{font features}" }, { t("\\displayfonttable["), i(1, "options"), t("]{"), i(2, "font name"), t("}{"), i(3, "font features"), t("}") }),
    S({ trig = "\\displayfonttable*", dscr = "{font name}{font features}" }, { t("\\displayfonttable*{"), i(1, "font name"), t("}{"), i(2, "font features"), t("}") }),
    S({ trig = "\\displayfonttable*", dscr = "[options]{font name}{font features}" }, { t("\\displayfonttable*["), i(1, "options"), t("]{"), i(2, "font name"), t("}{"), i(3, "font features"), t("}") }),
    S({ trig = "\\fonttablesetup", dscr = "{options}" }, { t("\\fonttablesetup{"), i(1, "options"), t("}") }),
    S({ trig = "\\fonttableglyphcount", dscr = "" }, { t("\\fonttableglyphcount") }),
  },
  keyvals = {
    {
      targets = "\\displayfonttable,\\displayfonttable*,\\fonttablesetup",
      lines = {
        "header#true,false",
        "noheader#true,false",
        "title-format=%<code%>",
        "title-format-cont=%<code%>",
        "display-block=#titles,rules,none",
        "hex-digits=#block,foot,head,head+foot,none",
        "hex-digits-font=%<font commands%>",
        "hex-digits-row-format=%<code%>",
        "color=#%color",
        "statistics#true,false",
        "nostatistics#true,false",
        "statistics-font=%<font commands%>",
        "statistics-format=%<code%>",
        "glyph-width=##L",
        "missing-glyph=%<code%>",
        "missing-glyph-font=%<font commands%>",
        "missing-glyph-color=#%color",
        "compare-with=%<font name%>",
        "compare-color=#%color",
        "compare-bgcolor=#%color",
        "statistics-compare-format=%<code%>",
        "range-start=%<slot%>",
        "range-end=%<slot%>",
      },
    },
  },
}
