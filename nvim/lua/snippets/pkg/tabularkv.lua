-- Generated by snipgen 1 from tabularkv.cwl; do not edit, regenerate.
-- source: TeXstudio completion/tabularkv.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: 7c23f5cf411ccee6a3a08f572307eb988b7c1356843937f611a9a120e42f864f
-- cwl-header:
--   # tabularht package
--   # Matthew Bertucci 10/1/2021 for v1.2
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "tabularkv",
  includes = { "keyval", "tabularht" },
  lists = {},
  snippets = {
    S({ trig = "\\tabularkv", dscr = "\\begin{tabularkv}{preamble}" }, { t("\\begin{tabularkv}{"), i(1, "preamble"), t({ "}", "\t" }), i(2), t({ "", "\\end{tabularkv}" }) }, "\\tabular"),
    S({ trig = "\\tabularkv", dscr = "\\begin{tabularkv}[options]{preamble}" }, { t("\\begin{tabularkv}["), i(1, "options"), t("]{"), i(2, "preamble"), t({ "}", "\t" }), i(3), t({ "", "\\end{tabularkv}" }) }, "\\tabular"),
    S({ trig = "\\tabularkv", dscr = "\\begin{tabularkv}[options][pos]{preamble}" }, { t("\\begin{tabularkv}["), i(1, "options"), t("]["), i(2, "pos"), t("]{"), i(3, "preamble"), t({ "}", "\t" }), i(4), t({ "", "\\end{tabularkv}" }) }, "\\tabular"),
  },
  keyvals = {
    {
      targets = "\\begin{tabularkv}",
      lines = {
        "width=##L",
        "x=##L",
        "height=##L",
        "valign=#top,bottom,center",
      },
    },
  },
}
