-- Generated by snipgen 1 from pgffor-ext.cwl; do not edit, regenerate.
-- source: TeXstudio completion/pgffor-ext.cwl at commit 0362907c21d1f725288bb553c5360d1179b78c1b (2026-09-12)
-- cwl-sha256: 3d16e23db2a61a719544ddd48f7d097b1f8c7e836c47535761225695cb5ab9fe
-- cwl-header:
--   # pgffor-ext package
--   # Matthew Bertucci 2025/03/18 for v0.6.0
-- licence: GPL-3; see NOTICE and COPYING beside the generated files

local ls = require("luasnip")
local s, t, i = ls.snippet, ls.text_node, ls.insert_node
local function S(ctx, nodes, cls)
  local sn = s(ctx, nodes)
  sn.cls = cls
  return sn
end

return {
  package = "pgffor-ext",
  includes = { "pgffor" },
  lists = {},
  snippets = {
    S({ trig = "\\tikzextset", dscr = "{options}" }, { t("\\tikzextset{"), i(1, "options"), t("}") }),
    S({ trig = "\\pgfextset", dscr = "{options}" }, { t("\\pgfextset{"), i(1, "options"), t("}") }),
  },
  keyvals = {
    {
      targets = "\\foreach#c",
      lines = {
        "ext/xparser={%<arg spec%>}{%<foreach value%>}",
        "ext/xparser Om",
      },
    },
    {
      targets = "\\foreach#c",
      lines = {
        "ext/use int=%<<start> to <end>%>",
        "ext/use int=%<<start> to <end> step <delta>%>",
        "ext/use float=%<<start> to <end>%>",
        "ext/use float=%<<start> to <end> step <delta>%>",
        "ext/no separator",
        "ext/normal list",
      },
    },
  },
}
